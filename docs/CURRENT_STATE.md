# Estado actual

Sprint 6 implementado en código el 20 de septiembre de 2026. Desktop y Web/PWA comparten Productos, Nueva venta e Historial mediante los mismos componentes y contratos. Desktop usa SQLite y conserva POS offline; Web usa Supabase autenticado y requiere conexión. **Las dos migraciones Cloud POS siguen pendientes de aplicación remota**, por lo que Web POS y sincronización POS aún no están operativos en el remoto.

- Customer Sync bidireccional conserva su implementación, esquema y flujo. El usuario confirmó pruebas manuales previas en ambos sentidos. Sus pruebas automatizadas siguen pasando.
- ProductCategory/Product Sync: Desktop registra altas y cambios locales en `pos_sync_outbox`; Cloud gestiona versiones `updated_at`; el push compara la última versión conocida y el pull usa cursor incremental por entidad. Conflictos de mismo UUID conservan la edición local y snapshot Cloud hasta elegir versión. Barcode único por negocio se mantiene en ambos esquemas.
- Sale/SaleItem Sync: las ventas locales mantienen UUID y snapshots. Cloud recibe el bundle mediante `complete_pos_sale`, una transacción idempotente; Desktop aplica un bundle Cloud mediante transacción SQLite. SaleItem viaja siempre junto a Sale, sin cursor independiente. Se retiran INSERT directos de ventas a `authenticated` en la nueva migración. No se añadieron operaciones de edición de ventas completadas.
- Web habilita Productos y categorías (listar, buscar, crear, editar, activar/desactivar), Nueva venta (búsqueda, input HID + Enter, alta rápida, carrito, checkout) e Historial con detalle. Desktop usa las mismas pantallas con repositorios SQLite. La UI obtiene el negocio de `BusinessContext`.
- El indicador de Desktop suma pendientes y conflictos de Customer + POS. El ciclo valida sesión/membership y ejecuta Customer → categoría → producto → venta. Sin sesión válida o conexión, Desktop sigue operando en SQLite y deja los cambios pendientes.
- `20260923000000_pos_mvp.sql` y `20260924000000_pos_sync.sql` son las migraciones Cloud pendientes. `0007_pos_sync.sql` es la nueva migración SQLite. No se editaron migraciones ya aplicadas.
- Verificaciones: `pnpm typecheck`, `pnpm lint`, `pnpm test` (48 pruebas), `pnpm build`, `pnpm format:check`, `pnpm build:desktop` (MSI y NSIS), `tests/pos_sqlite.py`, `tests/pos_sync_sqlite.py` y `tests/pos_cloud_fk.py` pasaron. `pnpm exec supabase db push --dry-run` enumeró solo las dos migraciones POS, sin seed ni roles; no ejecutó SQL.

No se ejecutó el flujo POS contra el proyecto Cloud real ni se probó scanner físico o una sesión offline en un equipo del local. Docker local no tiene daemon activo, así que la nueva función PostgreSQL y sus políticas no se ejecutaron contra una base de prueba. Inventory/Stock no fue implementado. El punto de reanudación está en `HANDOFF.md`; no avanzar a otro sprint sin solicitud nueva.
